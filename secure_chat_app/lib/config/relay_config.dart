/// Default relay server URL. Used when the user has not set a custom URL in settings.
/// For local testing this points to an ngrok tunnel; replace with your production URL when deploying.
const String kDefaultRelayBaseUrl =
    'https://unknowledgeable-guillermo-noninstrumentally.ngrok-free.dev';
